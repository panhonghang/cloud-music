<view style="
grid-template-columns: repeat(3,33.3%);
grid-template-rows: repeat({{lists.length/3}},300rpx);
" class="grid-box">
  <view a:for="{{lists}}">
    <view class="card">
      <image mode="aspectFit" src="{{item.picUrl}}"/>
    <text>{{item.name}}</text>
    </view>
  </view>
</view>